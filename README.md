# NeoOptimize

NeoOptimize — Windows system optimization UI (Electron + Vite + React).

Quick commands

- Dev UI: `npm run dev`
- Build UI: `npm run build` (outputs `dist/`)
- Run Electron (dev): `npm run electron:dev`
- Package Windows installer: `npm run package:win`

Notes

- This repository includes a Vite React UI which is bundled into an Electron shell.
- Release artifacts are created in `dist/` when packaging.

Recovered state

This repository was recovered and force-pushed to `origin/main` to restore lost files. A backup branch `recover-work` exists in the repository for safety.

CI

The project contains a GitHub Actions workflow to build the UI on push and pull requests.
# Magic Patterns - Vite Template

This code was generated by [Magic Patterns](https://magicpatterns.com) for this design: [Source Design](https://www.magicpatterns.com/c/szkqy3fkpbmnr521jnsqwj)

## Getting Started

1. Run `npm install`
2. Run `npm run dev`
