<Window x:Class="NeoOptimize.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="NeoOptimize"
        Height="900"
        Width="1440"
        MinHeight="760"
        MinWidth="1180"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Background="Transparent"
        Icon="Assets/icon.png">
    <Window.Resources>
        <Style x:Key="ShellButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#DDE8FF" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Margin" Value="6,0,0,0" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#172643" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border CornerRadius="28" BorderBrush="#294368" BorderThickness="1" Background="#07101D" Padding="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="56" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#0D172A" CornerRadius="27,27,0,0" MouseLeftButtonDown="TitleBar_OnMouseLeftButtonDown">
                <Grid Margin="18,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Source="Assets/icon.png" Width="28" Height="28" Margin="0,0,10,0" />
                        <StackPanel>
                            <TextBlock Text="NeoOptimize" Foreground="#EDF4FF" FontWeight="SemiBold" FontSize="15" />
                            <TextBlock Text="AI orchestration for Windows optimization" Foreground="#7D98BD" FontSize="11" />
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Style="{StaticResource ShellButtonStyle}" Click="Minimize_OnClick">_</Button>
                        <Button Style="{StaticResource ShellButtonStyle}" Click="MaximizeRestore_OnClick">▢</Button>
                        <Button Style="{StaticResource ShellButtonStyle}" Click="Close_OnClick">✕</Button>
                    </StackPanel>
                </Grid>
            </Border>

            <wv2:WebView2 x:Name="DashboardWebView" Grid.Row="1" />
        </Grid>
    </Border>
</Window>
